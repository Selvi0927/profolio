/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['benscott.dev'], // Add the external domain here
  },
};


export default nextConfig;

